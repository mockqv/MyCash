services:
  api:
    build:
      context: ./MyCash.API
      dockerfile: Dockerfile
    container_name: mycash-container
    env_file:
      - .env
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=${DATABASE_URL}
